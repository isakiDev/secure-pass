// const otpauth = authenticator.keyuri('test@gmail.com', 'mi-app', secret)

// // const qr = await qrcode.toDataURL(otpauth)

// qrcode.toString(otpauth, { type: 'terminal'}, (err, qr) => {
//   if531548 (err) return console.log('Error', err)

//   console.log(qr)
// })

const userInputCode = '599442'
const secret = 'OJVHOSSLMMCBWKDF'
